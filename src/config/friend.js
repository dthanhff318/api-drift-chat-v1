const friendStatusTypes = {
  NEW: "NEW",
  REQUEST: "REQUEST",
  ACCEPT: "ACCEPT",
};

const listFriendStatusTypes = Object.keys(friendStatusTypes);

module.exports = {
  friendStatusTypes,
  listFriendStatusTypes,
};
